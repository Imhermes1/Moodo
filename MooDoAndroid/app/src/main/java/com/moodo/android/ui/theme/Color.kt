package com.moodo.android.ui.theme

import androidx.compose.ui.graphics.Color

// MARK: - Calming Color Palette (matching iOS exactly)

// Soft, peaceful blue - primary accent color
val CalmingBlue = Color(red = 0.4f, green = 0.7f, blue = 0.9f)

// Gentle, nurturing green - success and completion  
val PeacefulGreen = Color(red = 0.5f, green = 0.8f, blue = 0.6f)

// Warm, encouraging yellow - today/current focus
val GentleYellow = Color(red = 1.0f, green = 0.85f, blue = 0.4f)

// Soft pink-violet - emotions and important items
val SoftViolet = Color(red = 0.85f, green = 0.6f, blue = 0.85f)

// Muted lavender - secondary accent
val MutedLavender = Color(red = 0.75f, green = 0.7f, blue = 0.9f)

// Sage green - alternative green for variety
val SageGreen = Color(red = 0.7f, green = 0.8f, blue = 0.7f)

// Dusty rose - warm, comforting pink
val DustyRose = Color(red = 0.9f, green = 0.75f, blue = 0.8f)

// Misty grey - neutral, calming
val MistyGrey = Color(red = 0.8f, green = 0.8f, blue = 0.85f)

// Soft cream - alternative to white
val SoftCream = Color(red = 0.98f, green = 0.96f, blue = 0.94f)

// MARK: - Semantic Colors

// Primary button and accent color
val PrimaryAccent = Color.Black

// Success states (completed tasks, positive actions)
val SuccessAccent = PeacefulGreen

// Warning/attention (today's tasks, priorities)
val WarningAccent = GentleYellow

// Emotional/important content
val EmotionalAccent = SoftViolet

// Secondary accent for variety
val SecondaryAccent = MutedLavender

// MARK: - Mental Health Focused Colors

// Calming meditation blue
val MeditationBlue = Color(red = 0.35f, green = 0.6f, blue = 0.8f)

// Grounding earth green
val GroundingGreen = Color(red = 0.45f, green = 0.7f, blue = 0.55f)

// Uplifting sunrise yellow
val SunriseYellow = Color(red = 0.95f, green = 0.8f, blue = 0.35f)

// Nurturing lavender
val NurturingLavender = Color(red = 0.7f, green = 0.65f, blue = 0.85f)

// Peaceful twilight
val PeacefulTwilight = Color(red = 0.6f, green = 0.55f, blue = 0.75f)

// MARK: - Background Gradient Colors

// Universal background gradient colors
val BackgroundYellow = Color(red = 1.0f, green = 0.95f, blue = 0.5f)
val BackgroundPink = Color(red = 1.0f, green = 0.7f, blue = 0.85f)
val BackgroundGreen = Color(red = 0.6f, green = 0.9f, blue = 0.6f)
val BackgroundBlue = Color(red = 0.5f, green = 0.8f, blue = 1.0f)

// Legacy Material 3 colors (kept for compatibility)
val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)

val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)
